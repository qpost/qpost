<div class="clearfix">
	<ul class="nav flex-column nav-pills">
		<li class="nav-item">
			<a class="nav-link" href="#"><i class="fas fa-arrow-left iconMargin-10"></i>Back to qpost</a>
		</li>

		<li class="dropdown-divider"></li>

		<li class="nav-item{{ navPoint in ["PROFILE_APPEARANCE"] ? " open" }}">
			<a class="nav-link" href="#"><i class="fas fa-user-circle iconMargin-10"></i>Profile</a>

			<a class="nav-link child{{ navPoint == "PROFILE_APPEARANCE" ? " active" }}" href="#">Appearance</a>
		</li>
		<li class="nav-item{{ navPoint in ["PREFERENCES_APPEARANCE","PREFERENCES_CONTENT"] ? " open" }}">
			<a class="nav-link" href="#"><i class="fas fa-cog iconMargin-10"></i>Preferences</a>

			<a class="nav-link child{{ navPoint == "PREFERENCES_APPEARANCE" ? " active" }}" href="#">Appearance</a>
			<a class="nav-link child{{ navPoint == "PREFERENCES_CONTENT" ? " active" }}" href="#">Content</a>
		</li>
		<li class="nav-item{{ navPoint in ["RELATIONSHIPS_FOLLOWING","RELATIONSHIPS_FOLLOWERS","RELATIONSHIP_BLOCKED"] ? " open" }}">
			<a class="nav-link" href="#"><i class="fas fa-users iconMargin-10"></i>Relationships</a>

			<a class="nav-link child{{ navPoint == "RELATIONSHIPS_FOLLOWING" ? " active" }}" href="#">Following</a>
			<a class="nav-link child{{ navPoint == "RELATIONSHIPS_FOLLOWERS" ? " active" }}" href="#">Followers</a>
			<a class="nav-link child{{ navPoint == "RELATIONSHIP_BLOCKED" ? " active" }}" href="#">Blocked</a>
		</li>
		<li class="nav-item{{ (navPoint in ["ACCOUNT_INFORMATION","ACCOUNT_USERNAME","ACCOUNT_PASSWORD","ACCOUNT_ACTIVE_SESSIONS"]) ? " open" }}">
			<a class="nav-link" href="#"><i class="fas fa-user iconMargin-10"></i>Account</a>

			<a class="nav-link child{{ navPoint == "ACCOUNT_INFORMATION" ? " active" }}" href="#">Information</a>
			<a class="nav-link child{{ navPoint == "ACCOUNT_USERNAME" ? " active" }}" href="#">Username</a>
			<a class="nav-link child{{ navPoint == "ACCOUNT_PASSWORD" ? " active" }}" href="#">Password</a>
			<a class="nav-link child{{ navPoint == "ACCOUNT_ACTIVE_SESSIONS" ? " active" }}" href="#">Active
				sessions</a>
		</li>
		<li class="nav-item{{ (navPoint in ["PRIVACY"]) ? " open" }}">
			<a class="nav-link{{ navPoint == "PRIVACY" ? " active" }}" href="#"><i
						class="fas fa-lock iconMargin-10"></i>Privacy</a>
		</li>

		<li class="dropdown-divider"></li>

		<li class="nav-item">
			<a class="nav-link" href="#"><i class="fas fa-sign-out-alt iconMargin-10"></i>Log out</a>
		</li>

		<li class="dropdown-divider"></li>
	</ul>

	<p class="mb-0 small text-muted">
		qpost v{{ _ENV["VERSION"] }}<br/>
		&copy; Gigadrive {{ "now"|date("Y") }}
	</p>
</div>