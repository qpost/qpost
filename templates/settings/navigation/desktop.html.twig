<div class="clearfix">
	<ul class="nav flex-column nav-pills">
		<li class="nav-item">
			<a class="nav-link" href="{{ path("qpost_home_index") }}"><i class="fas fa-arrow-left iconMargin-10"></i>Back
				to qpost</a>
		</li>

		<li class="dropdown-divider"></li>

		<li class="nav-item{{ navPoint in ["PROFILE_APPEARANCE"] ? " open" }}">
			<a class="nav-link" href="{{ path("qpost_settings_profileappearance") }}"><i
						class="fas fa-user-circle iconMargin-10"></i>Profile</a>

			<a class="nav-link child{{ navPoint == "PROFILE_APPEARANCE" ? " active" }}"
			   href="{{ path("qpost_settings_profileappearance") }}">Appearance</a>
		</li>
		<li class="nav-item{{ navPoint in ["PREFERENCES_APPEARANCE","PREFERENCES_CONTENT"] ? " open" }}">
			<a class="nav-link" href="{{ path("qpost_settings_preferencesappearance") }}"><i
						class="fas fa-cog iconMargin-10"></i>Preferences</a>

			<a class="nav-link child{{ navPoint == "PREFERENCES_APPEARANCE" ? " active" }}"
			   href="{{ path("qpost_settings_preferencesappearance") }}">Appearance</a>
			<a class="nav-link child{{ navPoint == "PREFERENCES_CONTENT" ? " active" }}"
			   href="{{ path("qpost_settings_preferencescontent") }}">Content</a>
		</li>
		<li class="nav-item{{ navPoint in ["RELATIONSHIPS_FOLLOWING","RELATIONSHIPS_FOLLOWERS","RELATIONSHIP_BLOCKED"] ? " open" }}">
			<a class="nav-link" href="{{ path("qpost_settings_relationshipsfollowing") }}"><i
						class="fas fa-users iconMargin-10"></i>Relationships</a>

			<a class="nav-link child{{ navPoint == "RELATIONSHIPS_FOLLOWING" ? " active" }}"
			   href="{{ path("qpost_settings_relationshipsfollowing") }}">Following</a>
			<a class="nav-link child{{ navPoint == "RELATIONSHIPS_FOLLOWERS" ? " active" }}"
			   href="{{ path("qpost_settings_relationshipsfollowers") }}">Followers</a>
			<a class="nav-link child{{ navPoint == "RELATIONSHIP_BLOCKED" ? " active" }}"
			   href="{{ path("qpost_settings_relationshipsblocked") }}">Blocked</a>
		</li>
		<li class="nav-item{{ (navPoint in ["ACCOUNT_INFORMATION","ACCOUNT_USERNAME","ACCOUNT_PASSWORD","ACCOUNT_ACTIVE_SESSIONS"]) ? " open" }}">
			<a class="nav-link" href="{{ path("qpost_settings_accountinformation") }}"><i
						class="fas fa-user iconMargin-10"></i>Account</a>

			<a class="nav-link child{{ navPoint == "ACCOUNT_INFORMATION" ? " active" }}"
			   href="{{ path("qpost_settings_accountinformation") }}">Information</a>
			<a class="nav-link child{{ navPoint == "ACCOUNT_USERNAME" ? " active" }}"
			   href="{{ path("qpost_settings_accountusername") }}">Username</a>
			<a class="nav-link child{{ navPoint == "ACCOUNT_PASSWORD" ? " active" }}"
			   href="{{ path("qpost_settings_accountpassword") }}">Password</a>
			<a class="nav-link child{{ navPoint == "ACCOUNT_ACTIVE_SESSIONS" ? " active" }}"
			   href="{{ path("qpost_settings_accountsessions") }}">Active
				sessions</a>
		</li>
		<li class="nav-item{{ (navPoint in ["PRIVACY"]) ? " open" }}">
			<a class="nav-link{{ navPoint == "PRIVACY" ? " active" }}" href="{{ path("qpost_settings_privacy") }}"><i
						class="fas fa-lock iconMargin-10"></i>Privacy</a>
		</li>

		<li class="dropdown-divider"></li>

		<li class="nav-item">
			<a class="nav-link" href="#"><i class="fas fa-sign-out-alt iconMargin-10"></i>Log out</a>
		</li>

		<li class="dropdown-divider"></li>
	</ul>

	<p class="mb-0 small text-muted">
		qpost v{{ _ENV["VERSION"] }}<br/>
		&copy; Gigadrive {{ "now"|date("Y") }}
	</p>
</div>