{% macro settingsHeadline(title) %}
	<h1 class="settingsHeadline">{{ title }}</h1>
	<hr class="settingsHeadlineSeparator"/>
{% endmacro %}

{% macro settingsHeadlineSmall(title) %}
	<h4 class="settingsHeadline">{{ title }}</h4>
{% endmacro %}